# Claude Code

Follow the repository instructions in `AGENTS.md`.

For code navigation, initialize the local CodeGraph index with:

```bash
scripts/setup_codegraph.sh
```

Use `scripts/setup_codegraph.sh --install-agent` only when the user wants this
checkout to update local agent MCP configuration.
